version: '3.8'

services:
 campspot-checker:
  platform: linux/amd64
  image: fredcorn/campspot-checker:amd64-latest
  build:
   context: .
  ports:
   - 49160:8787
  environment:
   - PORT=8787
  volumes:
   - ./:/app
   - /app/node_modules